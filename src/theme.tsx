import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ee7d8f",
    },
  },
  typography: {
    allVariants: {
      color: "white",
    },
  },
});

export default theme;
